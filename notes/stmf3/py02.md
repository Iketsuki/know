# Worksheet: Advanced Python Game Features

**Name:** ____________________________  **Class:** ________________

### Section 1: Working with Lists (Arrays)

*Fill in the blanks using the information about Python lists.*

1. A **List** is a special data structure that allows a programmer to create many variables without giving them individual __________.


2. Each individual position in a list starts from the index number __________.


3. To add a new item to the end of an existing list, we use the method: `.____________()`.


4. Lists are powerful because they allow programmers to handle a large number of variables efficiently using __________.


5. If we want to create 10 monsters, we first create an empty list called `monsters = []`, then use a loop to create a `temp_monster` and __________ it to the list.



---

### Section 2: The `update()` Function & Movement

*Explore how the game loop handles changes over time.*

1. While the `draw()` function refreshes the window, the **`update()`** function is responsible for changing the game state.


2. The `update()` function is called automatically __________ times every second.


3. To make a monster move, we must change its **(x, y) __________** inside the `update()` function.



---

### Section 3: Creating a Countdown Timer

*Follow the logic for adding a time limit to your game.*

1. To track remaining time, create a variable (e.g., `TIME`) and set it to a starting value like __________ seconds.


2. Since `update()` runs 60 times per second, each call represents approximately **0.0167 seconds**.


3. To decrease the timer, you should deduct __________ from the `TIME` variable every time `update()` is called.


4. Because `TIME` is defined outside the function, you must use the keyword `__________` inside `update()` before modifying it.

<div style="page-break-after: always;"></div>

---

### Section 4: Discovery – Displaying Text

*Look at the "Show the time on the screen" section and answer below.*

1. To display text (like the countdown or score) on the screen, we use the built-in object called **`__________`**.


2. Text display commands should be placed inside the `__________()` function so they are visible to the player.



**Challenge Question:** In your `draw()` function, the logic for 10 monsters should be: "If the monster is **not __________**, then draw it on the screen".

---

### Section 5: My Game Plan

Briefly list two features you want to add to your finished game (e.g., background music, a start button, or penalty points).

1. ---


2. ---
