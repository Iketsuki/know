## Tic-Tac-Toe: Revision Edition – Part 2 

Now that the board is built and the global variables are set, it’s time to focus on the "action" phase: player movement, winning logic, and the final results!

### Part 5: Player Interaction (`markChess`)

The `markChess(board, turn)` function is where the user interacts with your code. Since humans count from 1 to 9 but Python counts from 0 to 8, we have to bridge that gap.

**Discussion Questions:**

1.  **The Modulo Trick:** If the `turn` variable starts at 0 and increases by 1 each loop, we use `turn % 2` to pick the player. 
    * If `turn` is 4, what is the result of `4 % 2`? \_\_\_\_\_\_\_\_\_\_
    * Based on the `players` list `["X", "O"]`, which player does that represent? \_\_\_\_\_\_\_\_\_\_
2.  **Input Conversion:** The `input()` function always returns a **string**. What function must you use to turn that string into a number so you can use it as a list index?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
3.  **Bulletproofing:** Before allowing a move, how can you check if the chosen spot on the board has already been taken by an "X" or "O"?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

---

<div style="page-break-after: always;"></div>

### Part 6: The Logic Gate (`checkWinner` & `checkDraw`)

This is the "Ref" of your game. It decides when the `while` loop should stop.

**Task:** Match the logic to the correct board indices.

| To Check... | Indices to look at: |
| :--- | :--- |
| **Top Row** | 0, 1, \_\_ |
| **Middle Row** | \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_ |
| **First Column** | 0, 3, \_\_  |
| **The Diagonal ( \ )** | \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_ |

**Ponder:**
* Why is it important to check for a **winner** before checking for a **draw**? (Hint: What happens on the very last move, the 9th turn?)
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

---

### Part 7: Scoring and Results

After the loop ends, we must report the outcome and update the tally.

**Discussion Questions:**

1.  **Lists for Scores:** We use a list `[0, 0]` for scores. If "X" wins, which index in that list (0 or 1) should be incremented?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
2.  **Persistence:** Why do we define the `scores` list at the very top of the script instead of inside the `game()` function?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

---

### Part 8: The Tournament Loop (Avoiding Recursion)

Instead of calling `game()` inside itself (recursion), we use a **loop** to allow for multiple matches.

**Discussion Questions:**

1.  **The Loop Variable:** How can we use a variable like `playing = True` to keep the game going until the user chooses to quit?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
2.  **The Exit Strategy:** If the user enters "N" to the "Play again?" prompt, how do you stop the loop and say goodbye?
    > *Your Answer:* \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

---


<div style="page-break-after: always;"></div>

### Part 9: Filling in the Logic

Complete the code snippets below by filling in the blanks.

**A. Picking the Player**
```python
def markChess(board, turn):
    # Use modulo to alternate between index 0 and 1
    currentPlayer = players[________ % 2] 
    
    # Ask for input and adjust for 0-based indexing
    choice = int(input("Player " + ________ + ", choose (1-9): "))
    index = choice - ________ 
    
    # Place the mark on the board
    board[index] = ________
```

**B. Checking for a Draw**
```python
def checkDraw(turn):
    # A draw happens when the turn count reaches the maximum squares
    if turn >= ________: 
        return True
    else:
        return False
```

**C. Updating Scores**
```python
def updateScores(scores, winner):
    if winner == "X":
        scores[0] = scores[0] + 1
    elif winner == ________:
        scores[1] = ________ + 1
```
